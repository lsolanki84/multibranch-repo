pipeline {
        agent any
		 options{
		skipDefaultCheckout() 
	}
    stages {
        stage('Build') {
                    steps {
			            script {
			                sh "echo project1" 
			                }
                        }
		    }
    }
}
